# Public API base (no trailing slash). Vercel: set to your Render service URL, e.g. https://eu-ai-act-api.onrender.com
NEXT_PUBLIC_API_URL=http://127.0.0.1:8060

# Optional: default email shown in Admin login modal (must match API ADMIN_EMAIL if set)
# NEXT_PUBLIC_ADMIN_EMAIL=beaconone.org@gmail.com
